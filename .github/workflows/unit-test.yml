name: unit tests

on: pull_request

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: run unit tests
      working-directory: meta-secret-node
      run: cargo test
